// Longest Common Prefix



let longestCommonPrefix = function(arr) {
  for(let str of arr){
    console.log(str);
    
  }
}

let strs = ["flower", "flow", "flight"];

// longestCommonPrefix(strs)

// this to not an easy one to solve;

// will come back to this later;