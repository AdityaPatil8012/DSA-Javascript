// reverse the array:

let arr = [1,2,4,5,6,7,8];

// this is the easiest way to solve this problem using method;
function reverseArray(arr) {
   console.log(arr.reverse())
  // return arr.reverse()
}

// reverseArray(arr);

// let's see how we can solve by building our own logic;